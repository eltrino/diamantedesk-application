{% extends 'OroUIBundle:actions:update.html.twig' %}
{% form_theme form with ['OroFormBundle:Form:fields.html.twig'] %}
{% set formAction = path('diamante_ticket_create')  %}
{% block head_script %}
    {{ parent() }}

    {% block stylesheets %}
        {{ form_stylesheet(form) }}
    {% endblock %}
{% endblock %}

{% block navButtons %}
    {{ UI.button({'path' : path('diamante_ticket_list'), 'title' : 'eltrino.diamantedesk.ui.cancel'|trans, 'label' : 'eltrino.diamantedesk.ui.cancel'|trans}) }}
    {{ UI.saveAndStayButton() }}
    {{ UI.saveAndCloseButton() }}
{% endblock %}

{% block pageHeader %}
    {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'eltrino.diamantedesk.ticket.entity_label'|trans}) %}
    {% include 'OroUIBundle::page_title_block.html.twig' with { title: title } %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'ticketDetails' %}

    {% set dataBlocks = [{
        'title': 'eltrino.diamantedesk.common.general'|trans,
        'class': 'active',
        'subblocks': [
            {
                'title': 'eltrino.diamantedesk.ticket.details'|trans,
                'data': [
                    form_row(form.branch),
                    form_row(form.subject),
                    form_row(form.status),
                    form_row(form.files),
                    form_row(form.priority),
                    form_row(form.source),
                    form_row(form.reporter),
                    form_row(form.assignee)
                ]
            }
        ]
    }] %}

    {% set dataBlocks = dataBlocks|merge([{
        'title' : 'eltrino.diamantedesk.common.description'|trans,
        'subblocks': [
            {
                'title' : null,
                'data' : [
                    form_row(form.description)
                ]
            }]
        }]
    ) %}

    {% set data = {
        'formErrors': form_errors(form)? form_errors(form) : null,
        'dataBlocks': dataBlocks,
    } %}
    {{ parent() }}
{% endblock content_data %}
