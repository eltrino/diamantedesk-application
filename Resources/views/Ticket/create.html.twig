{% extends 'OroUIBundle:actions:update.html.twig' %}
{% form_theme form with ['OroFormBundle:Form:fields.html.twig'] %}
{% set formAction = path('diamante_ticket_create')  %}
{% block head_script %}
    {{ parent() }}

    {% block stylesheets %}
        {{ form_stylesheet(form) }}
    {% endblock %}
{% endblock %}

{% block navButtons %}
    {{ UI.button({'path' : path('diamante_ticket_list'), 'title' : 'Cancel'|trans, 'label' : 'Cancel'|trans}) }}
    {{ UI.saveAndStayButton() }}
    {{ UI.saveAndCloseButton() }}
{% endblock %}

{% block pageHeader %}
    {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'Ticket'|trans}) %}
    {% include 'OroUIBundle::page_title_block.html.twig' with { title: title } %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'ticketDetails' %}

    {% set dataBlocks = [{
        'title': 'General'|trans,
        'class': 'active',
        'subblocks': [
            {
                'title': 'Ticket Details'|trans,
                'data': [
                    form_row(form.branch),
                    form_row(form.subject),
                    form_row(form.status),
                    form_row(form.files),
                    form_row(form.reporter),
                    form_row(form.assignee)
                ]
            }
        ]
    }] %}

    {% set dataBlocks = dataBlocks|merge([{
        'title' : 'Description'|trans,
        'subblocks': [
            {
                'title' : null,
                'data' : [
                    form_row(form.description)
                ]
            }]
        }]
    ) %}

    {% set data = {
        'formErrors': form_errors(form)? form_errors(form) : null,
        'dataBlocks': dataBlocks,
    } %}
    {{ parent() }}
{% endblock content_data %}
