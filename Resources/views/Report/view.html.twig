{% extends 'DiamanteDeskBundle::layout.html.twig' %}

{% oro_title_set({params : {"%report.label%": page_title|default('diamante.desk.common.not_available'|trans) }}) %}

{% block content %}

    {% set options  = {
        data: data
    } %}
    <div class="container-fluid">
        <div class="diam-plot diam-{{ chart_type }}-widget"
             data-page-component-module="diamantedesk/js/app/components/{{ chart_type }}-widget-component"
             data-page-component-options="{{ options|json_encode }}"></div>

        {#{{ dump(data) }}#}
        {#{{ dump(chart_type) }}#}
    </div>

{% endblock content %}