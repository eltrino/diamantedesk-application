{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridName = 'diamante-automation-' ~ type ~ '-grid' %}

{% set pageTitle = 'diamante.automation.rule.entity_plural_label'|trans %}

{% block navButtons %}
    {% if resource_granted('CREATE', 'Entity:DiamanteAutomationBundle:' ~ type|capitalize ~ 'Rule') %}
        {{
        UI.addButton({
            'path' : path('diamante_automation_create', {'type': type}),
            'title' : 'diamante.automation.ui.create'|trans,
            'label' : 'diamante.automation.ui.create'|trans
        })
        }}
    {% endif %}
{% endblock %}