oro_system_configuration:
    groups:
        diamantedesk_setup:
            title: 'DiamanteDesk'
            priority: 30

        diamantedesk_channels:
            title: 'Channels'

        diamantedesk_channels_email_group:
            title: 'Email'
            icon: icon-envelope

    fields:
        eltrino_email_processing.mailbox_server_address:
            type: text
            priority: 95
            options:
                label: 'Server Address'

        eltrino_email_processing.mailbox_port:
            type: text
            priority: 90
            options:
                label: 'Port'
                tooltip: "It's typically on port 143. But IMAP over SSL is usually on port 993."

        eltrino_email_processing.mailbox_ssl:
            type: choice
            priority: 85
            options:
                label: 'SSL'
                choices:
                    - No
                    - Yes

        eltrino_email_processing.mailbox_username:
            type: text
            priority: 80
            options:
                label: 'Username'

        eltrino_email_processing.mailbox_password:
            type: password
            priority: 75
            options:
                label: 'Password'

    tree:
        system_configuration:
            platform:
                children:
                    diamantedesk_setup:
                        children:
                            diamantedesk_channels:
                                children:
                                    diamantedesk_channels_email_group:
                                        priority: 260
                                        children:
                                            - eltrino_email_processing.mailbox_server_address
                                            - eltrino_email_processing.mailbox_port
                                            - eltrino_email_processing.mailbox_ssl
                                            - eltrino_email_processing.mailbox_username
                                            - eltrino_email_processing.mailbox_password
