<thead>
  <tr>
    <th class="createdAt"><%= sorterState('createdAt') %> Date</th>
    <th class="subject"><%= sorterState('subject') %> Subject</th>
    <th class="priority"><%= sorterState('priority') %> Priority</th>
    <th class="status"><%= sorterState('status') %> Status</th>
  </tr>
</thead>
<tbody></tbody>