<thead>
  <tr>
    <th class="key sortable"><%= sorterState('key') %> Key</th>
    <th class="createdAt sortable"><%= sorterState('createdAt') %> Date</th>
    <th class="subject sortable"><%= sorterState('subject') %> Subject</th>
    <th class="priority sortable"><%= sorterState('priority') %> Priority</th>
    <th class="status sortable"><%= sorterState('status') %> Status</th>
  </tr>
</thead>
<tbody></tbody>