{{ delimiter }}

{{ header|trans }} by {{ user }}

{% if changes is defined %}
    {% for key, value in changes %}
        - {{ key|capitalize }}: {{ attribute(value, 'new')|capitalize }}
    {% endfor %}
{% endif %}

{% if attachments is defined %}
    Attachments:
    {% for key, value in attachments %}
        - {{ value }}
    {% endfor %}
{% endif %}

You can view this ticket {{ ticketKey }} in DiamanteDesk: {{ render_ticket_url(ticketKey, recipient) }}

- DiamanteDesk

